# Jeff Baraka — Portfolio

Personal portfolio for Baraka Jeff Gachango, software engineer (Kenya).

Open `index.html` in a browser, or serve locally:

```bash
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.
